[project]
name = "wox-plugin-killprocess"
version = "0.1.0"
description = "Kill process plugin for Wox"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["requests>=2.32.3", "wox-plugin>=0.0.1"]
